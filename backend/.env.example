# MongoDB Configuration
MONGO_URI=mongodb://localhost:27017/
DATABASE_NAME=finlit_db

# Flask Configuration
FLASK_SECRET_KEY=your-secret-key-change-this-in-production
PORT=5000

# Frontend Configuration
FRONTEND_URL=http://localhost:5173

# LLM Provider Configuration
# Choose one: "openai", "gemini", or "anthropic"
LLM_PROVIDER=openai

# API Keys (set based on LLM_PROVIDER)
# OpenAI (also used for voice features: Whisper STT, TTS, and embeddings)
OPENAI_API_KEY=sk-proj-your-key-here
OPENAI_MODEL=gpt-3.5-turbo  # or gpt-4

# Google Gemini
GEMINI_API_KEY=AIza-your-key-here
GEMINI_MODEL=gemini-pro

# Anthropic Claude
ANTHROPIC_API_KEY=sk-ant-your-key-here
ANTHROPIC_MODEL=claude-3-sonnet-20240229

# Optional: Logging
LOG_LEVEL=INFO

# ========== BUDGET VOICE SERVICES ==========
# Set to 'true' to use budget-friendly alternatives (saves ~60% on costs)
USE_BUDGET_SERVICES=false

# Deepgram (Speech-to-Text) - 28% cheaper than OpenAI Whisper
# Sign up at console.deepgram.com ($200 free credit)
DEEPGRAM_API_KEY=your-deepgram-key-here

# Google Cloud TTS - 73% cheaper than OpenAI TTS
# Setup: console.cloud.google.com → Create project → Enable Cloud Text-to-Speech API
# Option 1: Service account JSON file (recommended)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# Option 2: API key (alternative)
# GOOGLE_TTS_API_KEY=your-google-api-key

# Cloudflare R2 Storage - Free egress vs AWS S3
# Sign up at dash.cloudflare.com → R2 → Create bucket
R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-r2-access-key
R2_SECRET_ACCESS_KEY=your-r2-secret-key
R2_BUCKET_NAME=finlit-audio
R2_PUBLIC_URL=https://pub-xxx.r2.dev

# Local Embeddings - 100% free (no API key needed)
# Model downloads automatically on first use (~22MB)
# Run: python scripts/download_embedding_model.py
